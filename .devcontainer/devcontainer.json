{
  "name": "Robot Framework Dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.10",
  "postCreateCommand": "apt-get update && apt-get install -y xvfb && pip install --upgrade pip && pip install robotframework robotframework-browser && rfbrowser init",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "d-biehl.robotcode"
      ]
    }
  },
  "forwardPorts": [9222]
}

